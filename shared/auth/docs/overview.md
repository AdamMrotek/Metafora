# `shared-auth` — sign-in, session, roles

**No code yet.** What a role is and what it grants, plus session handling. Used
by `app-dashboard` and `app-studio`.

`app-call` deliberately does **not** use it. That is a rule, not an omission:
a patient arrives with a link and holds no account credential.
