# `store-config`

Accounts, roles, interview definitions and their versions, eval run records.
Readable by account holders. No patient data in it at all.

Today the interview definition is a constant in `services/agent/src/config/`
and there are no accounts. No migrations yet.
