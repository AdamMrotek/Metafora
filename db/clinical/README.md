# `store-clinical`

Patients, consent, interview results. Permission-gated per patient — the most
restricted thing we have, and the smallest possible number of readers.

Written through `svc-core` and nothing else. No migrations yet.
